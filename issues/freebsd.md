# FreeBSD

## Base system 

- Packages
    + Insufficient descriptions of packages.
    + No information on package contents.
    + Only viable source of information actual package contents.

- Man pages
    + Duplication of name and description: ntp.keys.5.gz
    + Input starts on second line utilities/{makefs,pcap_set_tstamp_precision,rdma_destroy_ep,lindebugfs,pcap_get_tstamp_precision,mixer}

- No man pages for a portion of base packages

- Netmap framework manpage in utilities package instead of in netmap package

- man3lua instead of man3 placement of manual pages for lua modules

